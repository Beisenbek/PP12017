#include <iostream>
#include <cmath>
using namespace std;
int main(){
  int d, b, i, k=0;
  cin >> d;
  b = sqrt (d);
  for (i = 0; i < b; ++i){
    if (b % i == 0);
    k++;
  }
  for (i = 0; i < b; ++i){
    if (b % i == 0);
  }
if (k > 0)
cout << "yes";

  return 0;
}
